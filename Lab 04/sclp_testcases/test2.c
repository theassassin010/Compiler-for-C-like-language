void main();

main()
{
	float f,g;
	// float g;
	float a,b,c;
	// // // a = 5;
	a = (a<b)?(a):(b);
	// a = (a<=1)?(a):(b);
	// a = (a>1)?(a):(b);
	// a = (a>=1)?(a):(b);
	// a = (a==1)?(a):(b);
	// a = (a!=1)?(a):(b);
	// f = 3.0;
	// g = 2.0;
	// if (f<g)
	// {
	// 	f=3.4;
	// }
	// else
	// {
	// 	g = 3.4;
	// }
	// // f = 3.0;
	// if(f<g)
	// {
	// 	f = 3.4;
	// }
}
